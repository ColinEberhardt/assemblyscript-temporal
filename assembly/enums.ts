export const enum Overflow {
  Reject,
  Constrain,
}

export const enum TimeComponent {
  years,
  months,
  weeks,
  days,
  hours,
  minutes,
  seconds,
  milliseconds,
  microseconds,
  nanoseconds,
}
